"use client";

export function Footer() {
  return (
    <footer className="glass-nav px-6 py-2 flex items-center justify-between" style={{ minHeight: 36 }}>
      <span className="text-xs text-slate-400">OMIX Agents — Public Demo</span>
      <span className="text-xs text-slate-400">Synthetic data only</span>
    </footer>
  );
}
